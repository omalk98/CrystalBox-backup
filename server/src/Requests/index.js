import PublicRequests from './public-requests';
import PrivateRequests from './private-requests';

const Requests = {
  Public: PublicRequests,
  Private: PrivateRequests
};

export default Requests;
